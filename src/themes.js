import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#b2ebf2',
      main: '#6db3a9',
      dark: '#006064',
    },
    secondary: {
      main: '#b36d78',
      light: '#e6c9d0',
      dark: '#932838',
    },
  },
});

export default theme;
