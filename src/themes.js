import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#ddf6f3',
      main: '#469b8e',
      dark: '#007d67',
    },
    secondary: {
      light: '#f6dde1',
      main: '#b97f82',
      dark: '#570016',
    },
  },
});

export default theme;
