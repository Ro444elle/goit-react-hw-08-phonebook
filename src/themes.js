import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#ddf6f3',
      main: '#469b8e',
      dark: '#007d67',
    },
    secondary: {
      light: '#b97f82',
      main: '#a34e53',
      dark: '#790008',
    },
  },
});

export default theme;
